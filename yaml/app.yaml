apiVersion: v1
kind: Pod
metadata:
  name: app
  labels:
    app: demo
    run: demo
spec:
  containers:
  - name: app
     image: gcr.io/daring-sunset-384809/demo:v1.0.0
     ports:
  - containerPort: 8080